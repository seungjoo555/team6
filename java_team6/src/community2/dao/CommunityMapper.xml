<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="community2.dao.CommunityDAO">
	<select id="selectBoardList" resultType="community2.model.vo.Board">
		select * from board
	</select>
	<insert id="insertPost">
	insert into post(po_bo_num, po_title, po_content, po_me_id) 
		values(#{post.po_bo_num}, #{post.po_title}, #{post.po_content}, #{post.po_me_id})
	</insert>
	<select id="selectPostList" resultType="community2.model.vo.Post">
		select * from post
	</select>
	<update id="updatePost">
		update post 
		set 
			po_bo_num = #{post.po_bo_num}, 
		    po_title = #{post.po_title}, 
		    po_content = #{post.po_content}, 
		    po_me_id = #{post.po_me_id}
		where 
			po_num = #{post.po_num};
	</update>
	<delete id="deletePost">
		delete from post where po_num = #{post.po_num}
	</delete>
	<update id="upView">
		update post set 
		    po_view = po_view + 1
		where 
			po_num = #{post.po_num};
	</update>
	<select id="selectPostListSearch" resultType="community2.model.vo.Post">
		SELECT 
		   *
		FROM
		    post
		WHERE
			po_title like concat(#{cri.search}, '%') or
			po_content like concat(#{cri.search}, '%')
		order by po_title desc
		LIMIT #{cri.pageStart}, #{cri.perPageNum}
	</select>
	<select id="selectPostContent" resultType="community2.model.vo.Post">
		select po_content as po_content from post where po_num = #{post.po_num}
	</select>
	<update id="updateView">
		update post 
		set 
		    po_view = '0'
		where 
			po_num = #{post.po_num};
	</update>
</mapper>